package dev.we3kings.nexusmoves.ui.review

/**
 * Scan review card — port of iOS ReviewItemsSheet. The modal IS the response
 * to a scan: check/uncheck detected items, commit via POST
 * /api/agents/nexus/inventory/commit keyed on scanId, Rescan button.
 * TODO(android-port): ModalBottomSheet; pill state updates in ChatScreen.
 */
