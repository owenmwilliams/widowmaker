package dev.we3kings.nexusmoves.ui.readiness

/**
 * "Get ready to share" — port of iOS ReadinessSheet: progress ring, warning
 * card, server-provided Next Steps (each row tappable → sends the step as the
 * user's chat message), Share CTA.
 * TODO(android-port).
 */
