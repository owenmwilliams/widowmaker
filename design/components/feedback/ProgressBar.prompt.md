Readiness progress bar with a shimmer fill and a looping sweep while the move is being built.

```jsx
<ProgressBar value={94} label="Ready to share" />
<ProgressBar value={0} label="Building your inventory" />
```

Label + percent sit above the track. The sweep hides at 0% and 100%.
