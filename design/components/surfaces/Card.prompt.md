The base surface for grouped content — dashboard panels, sheet sections, list groups.

```jsx
<Card><h3>Next steps</h3>…</Card>
<Card interactive onClick={openRoom}>…</Card>
```

18px radius, hairline border, soft shadow. Never use a colored left-border accent.
