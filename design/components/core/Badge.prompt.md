Small pill label for status, counts, or categories.

```jsx
<Badge tone="success" icon={<CheckIcon />}>8 added</Badge>
<Badge tone="beacon">Fragile</Badge>
```

Tones map to semantics: `success`, `warning`, `danger`, `accent`, `beacon` (warm emphasis), `neutral`.
