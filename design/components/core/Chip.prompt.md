Inline pill for filters, room selectors, or removable tokens.

```jsx
<Chip selected icon={<PinIcon />}>Hallway</Chip>
<Chip onRemove={() => {}}>Kitchen</Chip>
```

Selected = accent border + tint. For the tappable assistant action rows use `SuggestionButton` instead.
